# GenStdErrBands — Полосы стандартной ошибки (Standard Error Bands)
**Категория:** `OverlapStudies` (`Bands&Channels`) • **Тип:** Канальный индикатор на основе линейной регрессии  
💡 Индикатор отображается непосредственно на графике цен в той же шкале (PricePane).

```csharp
public static IList<double> GenStdErrBands(
    IList<double> price,
    int length,
    double bandsMultiplier,
    int bandMode,
    IContext context)
```

Полосы стандартной ошибки строят доверительный коридор вокруг линии линейной регрессии, где ширина полос определяется статистической мерой разброса цен — стандартной ошибкой оценки. В отличие от полос Боллинджера (основанных на стандартном отклонении), здесь волатильность измеряется через отклонение от тренда, что делает индикатор более чувствительным к изменению направленности движения. Применяется для оценки силы тренда и выявления точек разворота при выходе цены за границы коридора.

### Формула расчёта
```
Центральная линия = SMA₃(LR(price, length))
Верхняя полоса   = Центральная линия + bandsMultiplier × StdError
Нижняя полоса    = Центральная линия - bandsMultiplier × StdError
```
Где:
- `LR` — линейная регрессия за окно `length`
- `SMA₃` — сглаживание результатов регрессии скользящей средней (период 3)
- `StdError` — стандартная ошибка оценки для текущего окна
- Первые `length` значений результата равны 0 (недостаточно данных для расчёта)

### Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `price` | `IList<double>` | — | Исходный ценовой ряд (обычно `Close`, допустимы `High`/`Low`/`Open`) |
| `length` | `int` | — | Период окна для расчёта регрессии и стандартной ошибки. Чем больше — тем плавнее полосы, но выше запаздывание |
| `bandsMultiplier` | `double` | — | Множитель ширины коридора (2.0 ≈ 95% доверительный интервал). Увеличение расширяет полосы, снижая количество ложных пробоев |
| `bandMode` | `int` | — | Режим вывода: `1` — верхняя полоса, `2` — нижняя полоса, любое другое значение — центральная линия (линейная регрессия) |
| `context` | `IContext` | — | Контекст стратегии TSLab. Используется для кэширования промежуточных результатов (`context.GetData()`) и оптимизации производительности при переборе параметров |

### Результат расчёта
Возвращает `IList<double>` — коллекцию значений выбранной компоненты индикатора (верхняя/нижняя полоса или центральная линия). Запаздывание составляет `length` баров из-за оконного расчёта. Выход за границы коридора сигнализирует об усилении волатильности или возможном развороте тренда.

### Пример использования в TSLab
```csharp
using vvTSLtools;

// Расчёт всех трёх компонент индикатора
var upperBand = IndicatorsVV.GenStdErrBands(Close, 20, 2.0, 1, Context);
var lowerBand = IndicatorsVV.GenStdErrBands(Close, 20, 2.0, 2, Context);
var midLine   = IndicatorsVV.GenStdErrBands(Close, 20, 2.0, 0, Context);

// Торговая логика: пробой верхней полосы как сигнал роста волатильности
if (Close.LastValue > upperBand.LastValue)
{
    // Цена вышла за верхнюю границу — возможное ускорение восходящего движения
}
```
*Сборка `VitoIndicators.dll` подключается через меню «Сборки» в редакторе TSLab. Параметр `Context` доступен автоматически в методах стратегии (`OnBar`, `OnTick`).*

### Практическое применение
✅ **Для чего использовать**
- Оценка силы текущего тренда (сужение коридора → снижение волатильности → подготовка к импульсу)
- Фильтрация ложных пробоев уровней поддержки/сопротивления
- Визуализация зон переоценённости при выходе цены за границы полос
- Комбинация с другими индикаторами для подтверждения разворотов

💡 **Идеи для стратегий**
- Пробой верхней полосы + рост объёмов → сигнал к покупке
- Цена возвращается в коридор после выхода за границу → фиксация прибыли
- Сужение полос (снижение `StdError`) + breakout → вход в направлении пробоя

⚠️ **Ограничения**
- Менее эффективен во флэте с частыми ложными пробоями границ
- Требует подбора `length` под таймфрейм: на младших ТФ нужны меньшие периоды
- Стандартная ошибка чувствительна к выбросам (экстремальным барам)