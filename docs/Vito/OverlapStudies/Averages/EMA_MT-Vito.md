# EMA_MT — Экспоненциальная скользящая средняя (EMA)

**Категория:** `OverlapStudies` • **Тип:** Трендовый индикатор сглаживания  
*Постфикс «MT» указывает на адаптацию классического индикатора MetaTrader для платформы TSLab.*

---

## Сигнатура метода
```csharp
public static IList<double> EMA_MT(
    IList<double> sourceValues,
    double period,
    IContext context = null,
    bool useCache = true)
```

## Краткое описание
💡 **EMA (Exponential Moving Average)** — скользящая средняя с экспоненциальным взвешиванием, придающая больший вес недавним ценам по сравнению с классической SMA. Быстрее реагирует на изменения цены за счёт нелинейного распределения весов. Применяется для фильтрации шума, определения направления тренда и формирования динамических уровней поддержки/сопротивления.

## Формула расчёта
```
α = 2 / (period + 1)
EMA[i] = EMA[i-1] + α × (sourceValues[i] − EMA[i-1])
```
**Пояснение:**
- `α` — коэффициент сглаживания (чем меньше `period`, тем выше α и чувствительность)
- `EMA[i-1]` — значение индикатора на предыдущем баре
- `sourceValues[i]` — текущее значение исходной последовательности (обычно Close)

*Первое значение инициализируется первым элементом sourceValues[0].*

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `sourceValues` | `IList<double>` | — | Исходные данные: цены закрытия (Close), High/Low или другие ряды |
| `period` | `double` | — | Период сглаживания: уменьшение повышает чувствительность, но увеличивает шум |
| `context` | `IContext` | `null` | Контекст стратегии TSLab (бары, время, параметры). Требуется для кэширования через `context.GetData()` |
| `useCache` | `bool` | `true` | Включение кэширования результатов при оптимизации (требует передачи `context`) |

## Результат расчёта
Возвращает массив `IList<double>` значений EMA той же длины, что и `sourceValues`. Первое значение рассчитывается сразу (без периода ожидания), но **эффективное запаздывание** составляет ~`period/3` баров — значительно меньше, чем у SMA (`period/2`). Значения интерпретируются как динамическая линия тренда: цена выше EMA — восходящий импульс, ниже — нисходящий.

## Пример использования в TSLab
```csharp
using vvTSLtools;  // Подключение библиотеки VitoIndicators.dll

// Вариант 1: с кэшированием (рекомендуется для оптимизации)
var emaFast = IndicatorsVV.EMA_MT(Close, 9, Context, useCache: true);

// Вариант 2: без кэширования (для динамических расчётов)
var emaSlow = IndicatorsVV.EMA_MT(High, 50, context: null, useCache: false);

// Пример торговой логики
if (Close.LastValue > emaFast.LastValue && emaFast.LastValue > emaSlow.LastValue)
{
    // Бычья конфигурация: цена > EMA(9) > EMA(50)
    // ...
}
```
*Для работы требуется подключить сборку `VitoIndicators.dll` через меню «Сборки» в редакторе TSLab. Параметр `Context` передаётся автоматически в методах стратегии (например, в `OnBar`).*

## Практическое применение
✅ **Для чего использовать**
- Фильтрация краткосрочного шума в трендовых движениях
- Определение динамических уровней поддержки/сопротивления
- База для построения осцилляторов (например, MACD)
- Сигнализация разворотов при пробое/отскоке от линии

💡 **Идеи для стратегий**
1. **Пересечение цены и EMA**: покупка при закрытии выше EMA(20), продажа — ниже
2. **Двойная система**: быстрая EMA(9) пересекает медленную EMA(21) — сигнал тренда
3. **Отскок от тренда**: вход в лонг при отскоке цены от восходящей EMA(50) с подтверждением свечой поглощения

⚠️ **Ограничения**
- Генерирует ложные сигналы во флэте (боковике)
- Требует подбора периода под таймфрейм и волатильность актива

