# GenILRS — Интеграл наклона линейной регрессии (ILRS)
**Категория:** `MomentumIndicators` • **Тип:** Индикатор импульса на основе статистического анализа  
*Измеряет ускорение ценового движения через накопленный наклон регрессионной линии.*

```csharp
public static IList<double> GenILRS(
    IList<double> priceSeries,
    int period)
```

💡 ILRS (Integral of Linear Regression Slope) отслеживает динамику изменения наклона линейной регрессии, что позволяет выявлять ускорение или замедление тренда раньше классических осцилляторов. В отличие от простого наклона регрессии, интегральная форма сглаживает краткосрочные всплески и подчёркивает устойчивые изменения импульса. Индикатор особенно эффективен для фильтрации ложных пробоев в боковиках.

### Формула расчёта
```
slope = Σ[(xᵢ - x̄) * (yᵢ - ȳ)] / Σ[(xᵢ - x̄)²]
ILRS[bar] = Σ(slope) за период
```
Где:  
`xᵢ` — индексы баров в окне расчёта (0…period-1)  
`yᵢ` — цены из `priceSeries`  
`x̄`, `ȳ` — средние значения по окну  
Первые `period` значений копируются из исходной серии без изменений (недостаточно данных для регрессии).

### Параметры метода
| Параметр      | Тип             | Значение по умолчанию | Рекомендуемый Диапазон параметров от ... до | Описание                                                                                                                                        |
| ------------- | --------------- | --------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `priceSeries` | `IList<double>` | —                     |                                             | Исходная ценовая серия (обычно `Close`). Может использоваться `High`, `Low` или медианные цены для адаптации под волатильность.                 |
| `period`      | `int`           | —                     |                                             | Период расчёта регрессии. Увеличение периода снижает чувствительность, но уменьшает шум; уменьшение — повышает реакцию на локальные экстремумы. |

### Результат расчёта
Возвращает `IList<double>` — временную серию значений ILRS той же длины, что и входная. Запаздывание составляет `period` баров (минимальный размер окна для расчёта). Положительные значения указывают на нарастающий восходящий импульс, отрицательные — на ускорение нисходящего движения. Пересечение нулевой линии часто совпадает с сменой тренда.

### Пример использования в TSLab
```csharp
using vvTSLtools;

// Расчёт индикатора на цене закрытия с периодом 14
var ilrsValues = IndicatorsVV.GenILRS(Close, 14);

// Торговый сигнал: вход в лонг при ускорении восходящего импульса
if (ilrsValues[Count - 1] > ilrsValues[Count - 2] && 
    ilrsValues[Count - 2] > ilrsValues[Count - 3])
{
    // Устойчивый рост импульса — потенциальная точка входа
    OpenPosition(PositionType.Long);
}
```
*Сборка `VitoIndicators.dll` подключается через меню «Сборки» в редакторе TSLab. Метод не требует контекста стратегии — работает с чистыми массивами данных.*

### Практическое применение
✅ **Для чего использовать**
- Выявление ранних признаков смены тренда через замедление импульса
- Фильтрация ложных пробоев уровней поддержки/сопротивления
- Подтверждение силы тренда при работе с трендовыми индикаторами (например, скользящими средними)
- Адаптация чувствительности к волатильности через динамический `period`

💡 **Идеи для стратегий**

- Дивергенция: цена обновляет максимум, но ILRS формирует более низкий пик → сигнал разворота
- Кросс с сигнальной линией: сглаживание ILRS скользящей средней (3-5 периодов) для генерации точек входа
- Комбинация с объёмами: усиление сигнала при росте импульса на фоне увеличения объёмов
- Фильтр тренда: длинные позиции только при положительном и растущем ILRS

⚠️ **Ограничения**
- Слабо работает во флэте с частыми ложными пересечениями нуля
- Требует подбора периода под таймфрейм (на младших ТФ — 8-12, на дневных — 20-30)
- Не учитывает абсолютный уровень цены — используйте в связке с уровнями или трендовыми индикаторами