# GenStdError — Стандартная ошибка линейной регрессии (Standard Error)
**Категория:** `StatisticFunctions` • **Тип:** Статистическая мера разброса цен вокруг тренда  
*Индикатор оценивает точность линии линейной регрессии как описания ценового движения.*

```csharp
public static IList<double> GenStdError(
    IList<double> price,
    int period,
    IContext context)
```

💡 Стандартная ошибка (Standard Error) измеряет среднее отклонение фактических цен от линии линейной регрессии. Низкие значения указывают на чёткий тренд (цены плотно группируются вокруг регрессии), высокие — на волатильность или отсутствие направленного движения. В отличие от обычной волатильности (ATR), индикатор учитывает структуру тренда, а не просто размах цен.

### Формула расчёта
```
StdError = √(SSE / (period - 2))

где:
SSE — сумма квадратов ошибок (разница между ценой и значением регрессии)
period — длина окна расчёта
```
Первые `period - 1` значений ряда равны 0.0 (недостаточно данных для построения регрессии).

### Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `price` | `IList<double>` | — | Исходный ряд цен: `Close`, `Open`, `High`, `Low` или любой числовой ряд |
| `period` | `int` | — | Длина окна регрессии. Минимум 3 бара (из-за деления на `period - 2`). Рекомендуется 14–50 для баланса чувствительности и шумоподавления |
| `context` | `IContext` | — | Контекст стратегии TSLab: (1) доступ к данным баров, (2) автоматическое кэширование результатов через `context.GetData()` |

### Результат расчёта
Возвращает `IList<double>` — полный ряд значений стандартной ошибки. Запаздывание отсутствует (расчёт ведётся на текущем баре), но первые `period - 1` элементов равны 0.0. Интерпретация: чем меньше значение — тем стабильнее тренд; рост значения сигнализирует об усилении волатильности или развороте.

### Пример использования в TSLab
```csharp
using vvTSLtools;  // Подключение VitoIndicators.dll

// Расчёт стандартной ошибки по ценам закрытия с периодом 20
var stdError = IndicatorsVV.GenStdError(Close, 20, Context);

// Использование в торговой логике: фильтр по силе тренда
if (stdError[Count - 1] < stdError[Count - 2] * 0.8)
{
    // Тренд усиливается (разброс вокруг регрессии сокращается)
    // Можно рассматривать вход в направлении основного тренда
}
```
*Сборка `VitoIndicators.dll` подключается через меню «Сборки» в редакторе TSLab. Кэширование работает автоматически — повторный вызов с теми же `price` и `period` вернёт результат из кэша без пересчёта.*

### Практическое применение
✅ **Для чего использовать**
- Фильтрация сигналов: вход в сделку только при низкой стандартной ошибке (подтверждённый тренд)
- Обнаружение смены режима рынка: резкий рост значения = переход от тренда к флэту или всплеск волатильности
- Адаптивные стоп-лоссы: расширение зоны защиты при росте ошибки
- Подтверждение пробоев: пробой уровня сопровождается падением ошибки — сильнее сигнал

💡 **Идеи для стратегий**
- Комбинация с линейной регрессией: покупка при цене выше регрессии + падении стандартной ошибки
- Дивергенция: цена обновляет максимум, а стандартная ошибка растёт — слабость тренда
- Волатильностный фильтр: запрет торговли при ошибке выше 90-го перцентиля за последние 100 баров

⚠️ **Ограничения**
- Чувствителен к выбросам (одиночные всплески цены искажают расчёт)
- На коротких периодах (<10) даёт шумные значения
- Не определяет направление тренда — только его «чёткость»
- Требует дополнительной фильтрации в боковиках (ошибка постоянно высока)