# iStdError — Стандартная ошибка линейной регрессии (Std Error)
**Категория:** `ScalarCalc` • **Тип:** Статистический показатель точности регрессионной аппроксимации  

```csharp
public static double iStdError(
    IList<double> price,
    int period,
    int r,
    ref IList<double> _workErr)
```

💡 Стандартная ошибка линейной регрессии измеряет среднее отклонение цен от линии регрессии в заданном окне. Чем меньше значение — тем точнее линия описывает динамику цены. В отличие от стандартного отклонения, этот показатель учитывает трендовую составляющую через регрессионную модель. Применяется для оценки стабильности тренда и выявления фаз повышенной волатильности.

### Формула расчёта
```
SE = √[(SST - SSR) / (n - 2)]

где:
• SST = Σ(yᵢ - ȳ)² — общая сумма квадратов отклонений цены
• SSR = [Σ(xᵢ - x̄)(yᵢ - ȳ)]² / Σ(xᵢ - x̄)² — объяснённая сумма квадратов
• n — период расчёта (количество баров в окне)
• xᵢ — индекс времени (0, 1, 2...), x̄ — его среднее значение
• yᵢ — цена, ȳ — средняя цена в окне
```
*Примечание: при недостатке данных (n < 2) или отрицательной дисперсии остатков метод возвращает 0.0.*

### Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `price` | `IList<double>` | — | Массив цен (обычно Close) или значений индикатора для регрессионного анализа |
| `period` | `int` | — | Количество баров в окне регрессии. Увеличение периода снижает чувствительность, но повышает стабильность оценки |
| `r` | `int` | — | Индекс текущего бара, для которого рассчитывается ошибка |
| `_workErr` | `ref IList<double>` | — | Рабочий буфер для кэширования промежуточных значений. Должен быть предварительно инициализирован размером не менее количества баров |

### Результат расчёта
Возвращает скалярное значение `double` — стандартную ошибку регрессии на баре `r`. Запаздывание отсутствует (расчёт выполняется для текущего бара). Низкие значения указывают на стабильный, предсказуемый тренд; резкий рост ошибки сигнализирует о развороте или всплеске волатильности.

### Пример использования в TSLab
```csharp
using vvTSLtools;

// Инициализация рабочего буфера один раз при старте стратегии
private IList<double> workBuffer;

protected override void Init()
{
    workBuffer = new List<double>(Bars.Count);
    for (int i = 0; i < Bars.Count; i++) workBuffer.Add(0.0);
}

// Расчёт ошибки регрессии для текущего бара (без кэширования через Context)
var stdError = IndicatorsVV.iStdError(Close, 20, Bars.Count - 1, ref workBuffer);

// Применение в логике фильтрации сигналов
if (stdError < 0.5 * ATR[0]) // Тренд стабилен относительно текущей волатильности
{
    // Разрешить вход в сделку по основному сигналу
}
```
*Сборка `VitoIndicators.dll` подключается через меню «Сборки» в редакторе TSLab.*

### Практическое применение
✅ **Для чего использовать**
- Фильтрация ложных пробоев: игнорировать сигналы при высокой ошибке регрессии (> 1.5×ATR)
- Оценка качества тренда: низкая ошибка подтверждает устойчивость направленного движения
- Адаптивная настройка параметров: увеличивать период стоп-лосса при росте ошибки
- Выявление фаз консолидации: стабильно низкие значения ошибки при узком диапазоне цен

💡 **Идеи для применения**
- Комбинировать с наклоном линии регрессии: сильный тренд = высокий наклон + низкая ошибка
- Нормализовать ошибку относительно ATR для универсального порога срабатывания
- Использовать как весовой коэффициент при усреднении позиции (низкая ошибка → разрешить усреднение)