# GenLRMA — Линейная регрессионная скользящая средняя (LRMA)
**Категория:** `OverlapStudies` • **Тип:** Регрессионная скользящая средняя с минимальным запаздыванием  
*Реализация классического метода линейной регрессии через комбинацию скользящих средних для платформы TSLab.*

```csharp
public static IList<double> GenLRMA(
    IList<double> sourceValues,
    int period)
```

💡 LRMA (Linear Regression Moving Average) аппроксимирует линию линейной регрессии без прямого вычисления коэффициентов. Индикатор следует за ценой с меньшим запаздыванием по сравнению с классическими SMA/EMA, так как учитывает ускорение движения. Математически реализован через комбинацию взвешенной и простой скользящих средних.

### Формула расчёта
```
LRMA = 3.0 × WMA - 2.0 × SMA
```
Где:
- `WMA` — взвешенная скользящая средняя (GenWMA)
- `SMA` — простая скользящая средняя (GenSMA)
- Первые `period` значений инициализируются значениями WMA (недостаточно данных для полной регрессии)

### Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `sourceValues` | `IList<double>` | — | Исходная последовательность (обычно цены закрытия `Close`, реже — High/Low для волатильных инструментов) |
| `period` | `int` | — | Период аппроксимации: чем больше значение, тем сильнее сглаживание и запаздывание. Типичные значения: 14–21 для краткосрочной торговли |

### Результат расчёта
Возвращает `IList<double>` — коллекцию значений той же длины, что и входные данные. Первые `period` элементов содержат промежуточные значения (инициализация через WMA), стабильный расчёт начинается с индекса `period`. Индикатор следует за ценой плавнее, чем WMA, но с меньшим лагом, чем SMA.

### Пример использования в TSLab
```csharp
using vvTSLtools;

// Расчёт LRMA по ценам закрытия с периодом 20
var lrmaValues = IndicatorsVV.GenLRMA(Close, 20);

// Применение в торговой логике: пересечение цены и регрессионной линии
if (Close.LastValue > lrmaValues.LastValue && 
    Close[1] <= lrmaValues[lrmaValues.Count - 2])
{
    // Сигнал на покупку: цена пробила LRMA снизу вверх
    OpenPosition(PositionType.Long);
}
```
*Сборка `VitoIndicators.dll` подключается через меню «Сборки» в редакторе TSLab. Метод не требует контекста стратегии — работает с любыми числовыми последовательностями.*

### Практическое применение
✅ **Для чего использовать**
- Определение направления краткосрочного тренда без избыточного сглаживания
- Фильтрация ложных пробоев уровней поддержки/сопротивления
- Построение динамических каналов (например, ±2 стандартных отклонения от LRMA)
- Адаптивные стоп-лоссы, следующие за регрессионной линией

💡 **Идеи для стратегий**
- Пробой LRMA с подтверждением объёмом: вход при пересечении цены и индикатора + рост объёма > 20% от среднего
- Дивергенция цена/LRMA: при росте цены, но снижении наклона LRMA — сигнал разворота
- Комбинация с полосами Боллинджера: вход при касании нижней полосы + разворот цены к LRMA

⚠️ **Ограничения**
- На флэтах генерирует ложные сигналы из-за постоянных пересечений цены и линии
- Чувствителен к выбросам (спайкам) — рекомендуется предварительное сглаживание данных
- Требует подбора периода под таймфрейм: на 1-минутках эффективен период 8–12, на дневках — 25–50