# Wma — Взвешенная скользящая средняя (WMA)

**Категория:** `OverlapStudies` • **Тип:** Сглаживающий индикатор наложения

---
## Краткое описание

💡 WMA придаёт больший вес недавним ценам в расчёте среднего значения, в отличие от простой скользящей средней (SMA), где все бары имеют равный вес. Это делает индикатор более отзывчивым к текущим движениям цены при сохранении сглаживающих свойств. Идеален для фильтрации краткосрочного шума и раннего определения смены тренда.

## Формула расчёта
```
WMA = Σ(Price_i × Weight_i) / Σ(Weight_i)
где:
  - Weight_i = 1, 2, 3, ..., n (веса возрастают к текущему бару)
  - Σ(Weight_i) = n × (n + 1) / 2
  - n = optInTimePeriod
```

Пример для 3-барной WMA на ценах [10, 11, 12]:

Взвешенная сумма = (10×1) + (11×2) + (12×3) = 10 + 22 + 36 = 68
Сумма весов = 3×(3+1)/2 = 6
WMA = 68 / 6 ≈ 11.33

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд (обычно цены закрытия `Close`) |
| `inRange` | `Range` | весь массив | Диапазон индексов для расчёта. Если не указан — обрабатывается весь `inReal` |
| `outReal` | `Span<T>` | — | Выходной массив ТОЛЬКО с валидными значениями индикатора |
| `outRange` | `out Range` | — | Диапазон индексов `inReal`, для которых рассчитаны значения (`Start` = первый валидный бар) |
| `optInTimePeriod` | `int` | `30` | Период усреднения. Меньший период → выше чувствительность, но больше шума. Минимум: 2 |

## Результат расчёта
- Возвращает `RetCode.Success` при успешном расчёте
- Первое валидное значение появляется на баре с индексом `optInTimePeriod - 1` (lookback period)
- Выходной массив `outReal` содержит значения, привязанные к диапазону `outRange`:
  ```
  outReal[0] ↔ inReal[outRange.Start]
  outReal[i] ↔ inReal[outRange.Start + i]
  ```

## Практическое применение
✅ **Для чего использовать**
• Определение направления краткосрочного тренда с меньшим запаздыванием, чем у SMA
• Фильтрация ложных пробоев при использовании в качестве динамической поддержки/сопротивления
• Быстрое реагирование на развороты при комбинации с медленной SMA (например, WMA(10) + SMA(50))
• Сглаживание волатильности для входа в позицию по подтверждённому импульсу

💡 **Идеи для стратегий**
1. **Пересечение двух WMA**: сигнал на покупку при пересечении быстрой WMA(10) сверху медленной WMA(30); продажа — при обратном пересечении.
2. **Отскок от WMA**: вход в лонг при отскоке цены от восходящей WMA(20) с подтверждением бычьим паттерном свечи.

⚠️ **Ограничения**
• Чувствителен к выбросам (спайкам) из-за усиленного веса последних баров
• В боковике генерирует ложные сигналы из-за частых пересечений цены
• Требует подбора периода под таймфрейм: на младших ТФ лучше использовать периоды 5–15

## Сравнение с аналогами
| Индикатор | Запаздывание | Чувствительность | Шумоустойчивость |
|-----------|--------------|------------------|------------------|
| **WMA** | Среднее | Высокая | Средняя |
| **SMA** | Высокое | Низкая | Высокая |
| **EMA** | Низкое | Очень высокая | Низкая |

## Теги
#индикатор/наложение #скользящая_средняя #тренд #сглаживание #TALib #Wma
