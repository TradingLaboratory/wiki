# Ppo — Осциллятор процентной цены (PPO)

**Категория:** `MomentumIndicators` • **Тип:** Нормализованный осциллятор импульса на основе скользящих средних

---

## Краткое описание
💡 Индикатор отображает процентную разницу между быстрой и медленной скользящими средними, нормализуя импульс относительно текущего ценового уровня. В отличие от MACD, PPO выражается в процентах, что позволяет сравнивать силу импульса между инструментами с разными ценовыми диапазонами (например, акции за $10 и $1000). Предназначен для оценки силы тренда и выявления дивергенций.

## Формула расчёта
```math
PPO = ((FastMA - SlowMA) / SlowMA) * 100
```
Где:
- `FastMA` — скользящая средняя с периодом `optInFastPeriod`
- `SlowMA` — скользящая средняя с периодом `optInSlowPeriod`
- Тип скользящей средней определяется параметром `optInMAType`

Пример: при `FastMA = 105`, `SlowMA = 100` → `PPO = ((105 - 100) / 100) * 100 = 5%`

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной ценовой ряд (обычно цены закрытия `Close`) |
| `inRange` | `Range` | весь массив | Диапазон индексов для обработки входных данных |
| `optInFastPeriod` | `int` | `12` | Период быстрой скользящей средней. Уменьшение повышает чувствительность, но увеличивает шум |
| `optInSlowPeriod` | `int` | `26` | Период медленной скользящей средней. Увеличение снижает запаздывание, но сглаживает сигналы |
| `optInMAType` | `MAType` | `Sma` | Тип скользящей средней: `Sma`, `Ema`, `Wma` и др. EMA даёт меньшее запаздывание |
| `outReal` | `Span<T>` | — | Выходной массив только с валидными значениями индикатора |
| `outRange` | `out Range` | — | Диапазон индексов входных данных, для которых рассчитаны значения |

## Результат расчёта
- Возвращает `Core.RetCode` (статус выполнения) и заполняет `outReal` значениями в процентах
- Период запаздывания (lookback): `MaLookback(max(optInFastPeriod, optInSlowPeriod), optInMAType)`
- Первое валидное значение появляется после расчёта самой длинной скользящей средней
- Положительные значения = быстрая MA выше медленной (восходящий импульс), отрицательные — наоборот

## Практическое применение
✅ **Для чего использовать**
• Сравнение силы тренда между разными активами независимо от их цены
• Выявление дивергенций между ценой и импульсом (цена растёт, PPO падает → слабеющий тренд)
• Фильтрация ложных пробоев: подтверждение пробоя уровня ростом/падением PPO
• Оценка относительной силы тренда в процентах (например, +3% против +0.5%)

💡 **Идеи для стратегий**
1. **Пересечение нулевой линии**: сигнал на покупку при переходе PPO из отрицательной зоны в положительную (и наоборот для продажи)
2. **Дивергенция + подтверждение**: вход при бычьей дивергенции + пересечении скользящей средней самого индикатора

⚠️ **Ограничения**
• Слабо работает во флэте с частыми пересечениями нулевой линии
• Запаздывание увеличивается при использовании длинных периодов или SMA вместо EMA
• Не учитывает объёмы — требует комбинации с объёмными индикаторами для фильтрации сигналов

## Сравнение с аналогами
| Критерий | PPO | MACD |
|----------|-----|------|
| **Единицы измерения** | Проценты (%) | Абсолютные пункты |
| **Сравнимость активов** | ✅ Да (нормализован) | ❌ Нет |
| **Запаздывание** | Зависит от типа MA | Аналогично |
| **Интерпретация** | Относительная сила | Абсолютное отклонение |

## Теги
#индикатор/осциллятор #импульс #тренд #нормализация #TALib #Ppo