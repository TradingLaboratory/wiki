# MinMaxIndex — Индексы экстремумов (MinMax Index)

**Категория:** `MathOperators` • **Тип:** Оператор поиска позиций минимумов и максимумов в скользящем окне

---

## Краткое описание
💡 Индикатор возвращает **позиции** (индексы) минимальных и максимальных значений во входном массиве за заданный период, а не сами значения экстремумов. Отличается от аналогов `Min`/`Max` тем, что позволяет определить «возраст» экстремума — сколько баров прошло с момента его формирования. Используется для локализации точек разворота, анализа структуры ценового движения и построения адаптивных стратегий на основе позиционирования экстремумов.

## Формула расчёта
```
Окно расчёта для бара 'today': [trailingIdx, today]
где trailingIdx = today - (optInTimePeriod - 1)

outMinIdx[i] = argmin(inReal[k]), k ∈ [trailingIdx, today]
outMaxIdx[i] = argmax(inReal[k]), k ∈ [trailingIdx, today]
```
- `argmin`/`argmax` — функции поиска индекса минимального/максимального значения
- Индексы возвращаются относительно начала исходного массива `inReal`

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд: цены закрытия (Close), максимумы/минимумы (High/Low) или значения другого индикатора |
| `inRange` | `Range` | весь массив | Диапазон обработки входных данных (начальный и конечный индексы). Если не указан — обрабатывается весь массив |
| `outMinIdx` | `Span<T>` | — | Выходной массив индексов **минимальных** значений (позиции локальных впадин) |
| `outMaxIdx` | `Span<T>` | — | Выходной массив индексов **максимальных** значений (позиции локальных пиков) |
| `optInTimePeriod` | `int` | `30` | Длина скользящего окна в барах. Минимум: 2. Увеличение периода снижает чувствительность к шуму, но повышает запаздывание |

## Результат расчёта
- Возвращает код `RetCode`: `Success` (успех), `OutOfRangeParam` (неверный диапазон), `BadParam` (период < 2)
- **Lookback-период:** `optInTimePeriod - 1` — первое валидное значение появляется на баре с индексом `период-1`
- Выходной диапазон `outRange` указывает границы валидных значений в исходном массиве
- Индексы экстремумов позволяют рассчитать «возраст» минимума/максимума: `сегодняшний_бар - индекс_экстремума`

## Практическое применение
✅ **Для чего использовать**
• Определение свежих локальных минимумов/максимумов (возраст ≤ 3 баров)
• Поиск «провалов» цены к недавним минимумам для входа в лонг
• Анализ структуры тренда: чередование позиций экстремумов (высокие минимумы = восходящий тренд)
• Адаптивный стоп-лосс: размещение за индексом последнего минимума/максимума

💡 **Идеи для стратегий**
1. **Пробой свежего экстремума:**  
   Сигнал на покупку при закрытии выше значения `inReal[outMaxIdx[i]]`, где возраст максимума ≤ 2 бара.
2. **Дивергенция по позициям:**  
   Цена формирует новый максимум, но индекс максимума в окне 20 баров не обновляется — признак слабости тренда.

⚠️ **Ограничения**
• На боковике генерирует частые ложные сигналы из-за постоянного обновления экстремумов
• Требует дополнительной фильтрации (например, подтверждения объёмом или вторым индикатором)
• Не учитывает силу движения — только позицию экстремума

## Сравнение с аналогами
| Индикатор | Что возвращает | Когда использовать |
|-----------|----------------|---------------------|
| **MinMaxIndex** | Индексы экстремумов | Для анализа «возраста» и позиционирования точек разворота |
| **MinMax** | Значения экстремумов | Для построения ценовых каналов и уровней поддержки/сопротивления |
| **Min/Max** | Только минимум или максимум | При необходимости отслеживать один тип экстремума |

## Теги
#индикатор/математика #экстремумы #позиционирование #структура_трендов #TALib #MinMaxIndex