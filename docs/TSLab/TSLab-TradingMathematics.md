## Справочник кубиков TSLab: Trading Mathematics

Ниже представлена структурированная справочная страница по всем базовым кубикам раздела **Trading Mathematics** в визуальном конструкторе TSLab. Кубики сгруппированы по функциональному назначению для удобной навигации.

---

## 🔢 Математические операции

### Add (Сложить)
Суммирует несколько числовых значений (от 2 до 10 входов).

- **Входные параметры**: `double source1, double source2, ...` (2–10 источников)
- **Выход**: `double` — сумма входных значений
- **Описание**: Базовый кубик для арифметического сложения числовых рядов или констант.

---

### Sub (Вычесть)
Вычитает второе число из первого.

- **Входные параметры**: 
  - `double source1` — уменьшаемое
  - `double source2` — вычитаемое
- **Выход**: `double` — разность
- **Описание**: Выполняет операцию вычитания между двумя числовыми источниками.

---

### Multiply (Умножить на)
Умножает каждый элемент входной серии на заданный коэффициент.

- **Входные параметры**: `IList<double> bars`
- **Выход**: `double` — произведение
- **Описание**: Масштабирует числовую серию на константный множитель.

---

### Min (Наименьшее)
Находит минимальное значение среди нескольких входов (от 2 до 10).

- **Входные параметры**: `double source1, double source2, ...` (2–10 источников)
- **Выход**: `double` — минимальное значение
- **Описание**: Возвращает наименьшее из переданных числовых значений.

---

### Max (Наибольшее)
Находит максимальное значение среди нескольких входов (от 2 до 10).

- **Входные параметры**: `double source1, double source2, ...` (2–10 источников)
- **Выход**: `double` — максимальное значение
- **Описание**: Возвращает наибольшее из переданных числовых значений.

---

### Ln (Ln)
Вычисляет натуральный логарифм для серии значений.

- **Входные параметры**: `IList<double> bars`
- **Выход**: `IList<double>` — логарифмированный ряд
- **Описание**: Применяет функцию `ln(x)` к каждому элементу входной числовой серии.

---

## 🔣 Логические операции

### And (И)
Логическое «И» для нескольких булевых источников (от 2 до 30 входов).

- **Входные параметры**: `IList<bool> source1, IList<bool> source2, ...` (2–30 источников)
- **Выход**: `IList<bool>` — `true`, только если все входы одновременно `true`
- **Примеры**: [Скрипт с операцией AND](http://www.tslab.ru/files/script/script_AND.xml)

---

### Or (ИЛИ)
Логическое «ИЛИ» для нескольких булевых источников (от 2 до 30 входов).

- **Входные параметры**: `IList<bool> source1, IList<bool> source2, ...` (2–30 источников)
- **Выход**: `bool` — `true`, если хотя бы один вход `true`
- **Примеры**: [Пример по RSI и Bollinger](http://www.tslab.ru/files/script/script_AND.xml)

---

### Not (НЕ)
Логическое отрицание входного булевого значения.

- **Входные параметры**: `IList<bool> src`
- **Выход**: `bool` — инвертированное значение (`true` → `false`, `false` → `true`)
- **Примеры**: [Модифицированная стратегия Hi-Low](http://www.tslab.ru/files/script/Hi_Lo_1pos.xml)

---

### EqualHandler (Равно)
Сравнивает два числовых ряда на равенство (с учётом точности вычислений для `double`).

- **Входные параметры**: 
  - `IList<double> source1`
  - `IList<double> source2`
- **Выход**: `bool` — `true`, если значения равны

---

### GreaterHandler (Больше)
Определяет моменты, когда первый вход строго больше второго.

- **Входные параметры**: 
  - `IList<double> source1`
  - `IList<double> source2`
- **Выход**: `bool` — `true`, если `source1 > source2`

---

### GreaterOrEqualHandler (Больше или равно)
Определяет моменты, когда первый вход больше или равен второму.

- **Входные параметры**: 
  - `IList<double> source1`
  - `IList<double> source2`
- **Выход**: `bool` — `true`, если `source1 >= source2`

---

### LessHandler (Меньше)
Определяет моменты, когда первый вход строго меньше второго.

- **Входные параметры**: 
  - `IList<double> source1`
  - `IList<double> source2`
- **Выход**: `IList<bool>` — `true`, если `source1 < source2`

---

### LessOrEqualHandler (Меньше или равно)
Определяет моменты, когда первый вход меньше или равен второму.

- **Входные параметры**: 
  - `IList<double> source1`
  - `IList<double> source2`
- **Выход**: `IList<bool>` — `true`, если `source1 <= source2`

---

## 📊 Работа с данными баров

### Open (Открытие)
Возвращает цену открытия бара.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — цена открытия
- **Описание**: Извлекает значение `Open` из структуры бара.

---

### High (Максимум)
Возвращает максимальную цену бара.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — максимальная цена
- **Примеры**: [Стратегия Hi-Low](http://www.tslab.ru/files/script/Hi_Lo.xml)

---

### Low (Минимум)
Возвращает минимальную цену бара.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — минимальная цена
- **Примеры**: [Стратегия Hi-Low](http://www.tslab.ru/files/script/Hi_Lo.xml)

---

### Close (Закрытие)
Возвращает цену закрытия бара.

- **Входные параметры**: `ISecurity source`
- **Выход**: `double` — цена закрытия
- **Примеры**: [Пример стратегии 2MA](http://www.tslab.ru/files/script/2MA.xml)

---

### Volume (Объем)
Возвращает объём торгов (количество контрактов/лоты) за период бара.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — объём
- **Описание**: Количество операций с фьючерсными контрактами или опционами за период.

---

### Time (Время)
Преобразует время бара в числовой формат ЧЧММСС.

- **Входные параметры**: `ISecurity source`
- **Выход**: `IList<double>` — время в формате `hhmmss` (например, 10:31:45 → 103145)

---

### TimeInMins (Время в минутах)
Преобразует время бара в количество минут от начала дня.

- **Входные параметры**: `ISecurity source`
- **Выход**: `IList<double>` — минуты от полуночи (например, 10:31 → 631)

---

### Date (Дата)
Преобразует дату бара в числовой формат ГГММДД.

- **Входные параметры**: `ISecurity source`
- **Выход**: `IList<double>` — дата в формате `yymmdd`

---

### DayOfMonth (День месяца)
Возвращает день месяца для каждого бара (1–31).

- **Входные параметры**: `ISecurity source`
- **Выход**: `IList<double>` — номер дня месяца

---

### DayOfWeek (День недели)
Возвращает день недели для каждого бара (1 = понедельник, 7 = воскресенье).

- **Входные параметры**: `ISecurity source`
- **Выход**: `IList<double>` — номер дня недели

---

### Bid (Цена лучшей покупки)
Возвращает цену лучшей заявки на покупку (bid) на момент закрытия бара.

- **Входные параметры**: `ISecurity source`
- **Выход**: `double` — цена bid

---

### BidQty (Суммарный спрос)
Возвращает совокупный объём заявок на покупку (в лотах).

- **Входные параметры**: `ISecurity source`
- **Выход**: `double` — суммарный спрос

---

### Ask (Цена лучшей продажи)
Возвращает цену лучшей заявки на продажу (ask) на момент закрытия бара.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — цена ask

---

### AskQty (Суммарное предложение)
Возвращает совокупный объём заявок на продажу (в лотах).

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — суммарное предложение

---

### StepPrice (Стоимость шага цены)
Возвращает стоимость минимального шага цены для инструмента.

- **Входные параметры**: `IList<double> source`
- **Выход**: `double` — размер шага цены

---

### OpenInterest (Открытый интерес)
Возвращает объём открытых позиций (актуально для фьючерсов и опционов).

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — открытый интерес

---

### TheoreticalPrice (Теор. цена опциона)
Возвращает теоретическую цену опциона, рассчитанную биржей.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — теоретическая цена
- **Описание**: Значение совпадает с отображаемым в таблице «Котировки».

---

### OptVolatility (Опционная волатильность)
Возвращает теоретическую волатильность опциона от биржи.

- **Входные параметры**: `IDataBar bar`
- **Выход**: `double` — implied volatility

---

## 📈 Работа с сигналами

### CrossOver (Пересечение сверху)
Определяет моменты, когда второй ряд (сигнал) пересекает первый ряд (опорную линию) сверху вниз.

- **Входные параметры**: 
  - `IList<double> src1` — опорная линия
  - `IList<double> src2` — сигнал
- **Выход**: `IList<bool>` — `true` при пересечении сверху вниз
- **Примеры**: [Индикатор MACD](http://www.tslab.ru/files/script/MACD.xml)

---

### CrossUnder (Пересечение снизу)
Определяет моменты, когда второй ряд (сигнал) пересекает первый ряд (опорную линию) снизу вверх.

- **Входные параметры**: 
  - `IList<double> src1` — опорная линия
  - `IList<double> src2` — сигнал
- **Выход**: `IList<bool>` — `true` при пересечении снизу вверх
- **Примеры**: [Индикатор MACD](http://www.tslab.ru/files/script/MACD.xml)

---

### Flip (Перевернуть)
Имитирует триггер с двумя входами: при `true` на первом входе выход становится `true` до появления `true` на втором входе (и наоборот).

- **Входные параметры**: 
  - `IList<bool> source1` — вход включения
  - `IList<bool> source2` — вход выключения
- **Выход**: `bool` — переключаемое состояние
- **Особенность**: При одновременном `true` на обоих входах приоритет у второго (выключение).

---

### Hold (Задержать)
Задерживает входящий логический сигнал на N свечей (параметр «Период»).

- **Входные параметры**: `IList<bool> source`
- **Выход**: `IList<bool>` — задержанный сигнал
- **Описание**: Полезен для формирования сигналов с минимальной длительностью.

---

## 🔘 Константы и генераторы

### ConstGen (Константа)
Генерирует числовой ряд из постоянного значения.

- **Входные параметры**: `double Value`
- **Выход**: `IList<double>` — ряд констант
- **Примеры**: [Индикатор Stochastic K](http://www.tslab.ru/files/script/StochK.xml)

---

### BoolConst (Логическая константа)
Генерирует булев ряд из постоянного значения (`true`/`false`).

- **Входные параметры**: —
- **Выход**: `IList<bool>` — ряд логических констант

---

### StringConst (Строковая константа)
Генерирует строковый ряд из заданной константы.

- **Входные параметры**: —
- **Выход**: `IList<string>` — ряд строковых констант

---

### TextHandler (Текст)
Блок без входов с редактируемым текстовым параметром, возвращающим его как результат.

- **Входные параметры**: —
- **Выход**: `IList<string>` — заданный текст
- **Описание**: Используется для передачи статических строковых значений в другие кубики.

---

### ControlledBoolConst (Управляемая логическая константа)
Переключатель: при `true` на входе возвращает значение из поля «Значение», при `false` — из поля «Значение по умолчанию».

- **Входные параметры**: `IList<bool> source`
- **Выход**: `IList<bool>` — управляемая константа

---

### ResettableControlledBoolConst (Сбрасываемая управляемая логическая константа)
Расширенная версия переключателя с возможностью сброса состояния.

- **Входные параметры**: 
  - `IList<bool> source`
  - `IList<bool> resetValues`
- **Выход**: — управляемая константа с функцией сброса

---

## 🧩 Работа с ценными бумагами и синтетическими инструментами

### BarsConstructorHandler (Конструктор баров)
Создаёт синтетический инструмент из 5 числовых рядов (Open, Close, High, Low, Volume).

- **Входные параметры**: 
  - `IList<double> openList`
  - `IList<double> closeList`
  - `IList<double> highList`
  - `IList<double> lowList`
  - `IList<double> volumeList`
- **Выход**: `ISecurity` — синтетический инструмент
- **Примеры**: [Модифицированный пример MACD](http://www.tslab.ru/files/script/MACD-BarsHeld.tscript)

---

### SecMultiply (Умножить (ЦБ) на)
Умножает все цены баров входного инструмента на заданный коэффициент.

- **Входные параметры**: `ISecurity source`
- **Выход**: `ISecurity` — синтетический инструмент с масштабированными ценами

---

### SecMultiplyWith (Перемножить с)
Умножает каждый бар входного инструмента на соответствующий вес из второго числового ряда.

- **Входные параметры**: 
  - `ISecurity source`
  - `IList<double> src2` — ряд весов
- **Выход**: `ISecurity` — синтетический инструмент

---

### SecDivideWith (Поделить с)
Делит каждый бар входного инструмента на соответствующий вес из второго числового ряда.

- **Входные параметры**: 
  - `ISecurity source`
  - `IList<double> src2` — ряд делителей
- **Выход**: `ISecurity` — синтетический инструмент

---

### Compress / CompressAdvanced / CompressToSeconds (Сжать)
Сжимает временной диапазон баров в более крупный таймфрейм (минуты → часы, дни → недели и т.д.).

- **Входные параметры**: `ISecurity source`
- **Выход**: `ISecurity` — сжатый ряд баров
- **Особенности**: 
  - `Compress` — базовая версия
  - `CompressAdvanced` — расширенные параметры сжатия
  - `CompressToSeconds` — сжатие в секундные интервалы
- **Примеры**: [MACD-BarsHeld](http://www.tslab.ru/files/script/MACD-BarsHeld.xml)

---

### Decompres / DecompresBool (Разжать)
Восстанавливает сжатые данные (числовые или логические) в оригинальный временной диапазон.

- **Входные параметры**: 
  - `ISecurity security` — исходный инструмент (для определения периода)
  - `IList<double>` или `IList<bool> source` — сжатые данные
- **Выход**: 
  - `IList<double>` — для числовых данных
  - `IList<bool>` — для логических данных
- **Примеры**: [MACD-BarsHeld](http://www.tslab.ru/files/script/MACD-BarsHeld.xml)

---

## 🔁 Прочие полезные кубики

### BarNumber (Номер бара)
Возвращает индекс текущего бара в серии (нумерация с 0).

- **Входные параметры**: `ISecurity security`
- **Выход**: `IList<double>` — номер бара

---

### PrevValue (Предыдущее значение)
Возвращает значение из входного ряда, сдвинутое на N шагов назад.

- **Входные параметры**: `IList<double> source`
- **Выход**: `double` — предыдущее значение

---

### Shift (Сдвиг)
Сдвигает значения числового ряда на N свечей вправо (заполняя начало `NaN`).

- **Входные параметры**: `IList<double> source`
- **Выход**: `IList<double>` — сдвинутый ряд

---

### RandomHandler (Случайное число)
Генерирует случайные числа в заданном диапазоне для каждого бара.

- **Входные параметры**: `ISecurity sec`
- **Выход**: — случайное значение (зависит от настроек диапазона)

---

### QuoteByName (Котировка по имени)
Получает значение из таблицы «Котировки» по имени инструмента.

- **Входные параметры**: `ISecurity sec`
- **Выход**: значение из таблицы котировок

---

### OrderBookPrice / OrderBookQty / OrderBookTotal
Работают с данными стакана заявок (актуально при наличии подписки на стакан):

- **OrderBookPrice** — возвращает цену на заданной позиции стакана
- **OrderBookQty** — возвращает объём на заданной позиции стакана
- **OrderBookTotal** — возвращает суммарный объём в первых N строках стакана
- **Входные параметры**: `ISecurity sec, int barNum`
- **Выход**: `double`

---

### AbsolutCommission (Абсолютная комиссия)
Рассчитывает комиссию за сделку в абсолютных величинах.

- **Входные параметры**: `ISecurity source`
- **Выход**: — значение комиссии
- **Описание**: Фиксированная комиссия на одну сделку (покупка или продажа).

---

### RelativeCommission (Относительная комиссия)
Рассчитывает комиссию за сделку в процентах от объёма.

- **Входные параметры**: 
  - `IPosition pos`
  - `double price`
  - `double shares`
  - `bool isEntry`
  - `bool isPart`
- **Описание**: Учитывает стоимость заёмных средств для коротких позиций.

---

### RelativeCommisionWithMinimal (Относ. комиссия с минимумом)
Расширенная версия относительной комиссии с учётом минимального абсолютного значения.

- **Входные параметры**: аналогично `RelativeCommission`
- **Описание**: Гарантирует, что комиссия не будет ниже заданного минимума.

---

### ResultForOptimization (Результат для оптимизации)
Сохраняет пользовательские метрики для последующей оптимизации стратегии.

- **Входные параметры**: `IList<double> values`
- **Описание**: Используется для передачи кастомных показателей в модуль оптимизации.
