# Mom — Моментум (MOM)

**Категория:** `MomentumIndicators` • **Тип:** Осциллятор скорости изменения цены

---

## Краткое описание
💡 Моментум (Momentum) измеряет **абсолютную разницу** между текущей ценой и ценой `optInTimePeriod` периодов назад. В отличие от нормализованных аналогов (например, ROC), MOM показывает изменение в пунктах, а не в процентах. Простой индикатор для оценки силы и ускорения ценового движения: положительные значения — восходящий импульс, отрицательные — нисходящий.

## Формула расчёта
```math
MOM[t] = Close[t] - Close[t - n]
```
Где:
- `Close[t]` — текущая цена закрытия
- `Close[t - n]` — цена закрытия `n` периодов назад (`n = optInTimePeriod`)
- Результат выражается в абсолютных пунктах цены

Пример (период 10): если цена сегодня 150.50, а 10 дней назад — 145.20, MOM = +5.30 пункта.

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд (обычно цены закрытия `Close`) |
| `inRange` | `Range` | весь массив | Диапазон индексов входных данных для расчёта |
| `outReal` | `Span<T>` | — | Выходной массив с валидными значениями индикатора |
| `outRange` | `out Range` | — | Диапазон индексов входных данных, для которых рассчитаны значения |
| `optInTimePeriod` | `int` | `10` | Период сравнения: чем меньше — выше чувствительность к краткосрочным движениям, но больше шума |

## Результат расчёта
- **Возврат:** `outReal` содержит абсолютные изменения цены в пунктах; `outRange` указывает диапазон валидных значений во входном массиве.
- **Lookback period:** `optInTimePeriod` (первое значение появляется на баре №`n`, где `n` — период расчёта).
- **Интерпретация:** значения выше нуля — восходящий импульс, ниже нуля — нисходящий. Пересечение нулевой линии часто используется как сигнал смены тренда.

## Практическое применение
✅ **Для чего использовать**
- Выявление ускорения/замедления тренда
- Поиск дивергенций (расхождения цены и импульса)
- Фильтрация ложных пробоев (слабый импульс при пробое = риск отката)
- Оценка силы движения без привязки к процентам

💡 **Идеи для стратегий**
1. **Пересечение нуля:** покупка при переходе MOM из минуса в плюс, продажа — наоборот.
2. **Дивергенция:** цена обновляет максимум, а MOM формирует более низкий пик → сигнал разворота вниз.
3. **Экстремумы:** фиксация прибыли при достижении исторических максимумов/минимумов MOM (перекупленность/перепроданность в абсолютных значениях).
4. **Фильтр тренда:** длинные позиции только при положительном MOM на старшем ТФ.

⚠️ **Ограничения**
- Не нормализован: нельзя сравнивать значения для разных инструментов или одного инструмента с разной волатильностью.
- Сильно зависит от масштаба цены (акции за $1000 vs $10).
- На флэтах генерирует ложные сигналы из-за шума вокруг нулевой линии.

## Сравнение с аналогами
| Критерий | MOM | ROC | RSI |
|----------|-----|-----|-----|
| **Тип значения** | Абсолютное (пункты) | Процентное (%) | Нормализовано (0–100) |
| **Сравнение инструментов** | ❌ Невозможно | ✅ Возможн | ✅ Возможн |
| **Чувствительность к волатильности** | Высокая | Средняя | Низкая |
| **Интерпретация нуля** | Смена направления | Смена направления | Не применимо |

## Теги
#индикатор/импульс #моментум #скорость_изменения #TALib #Mom