# PlusDM — Положительное направленное движение (+DM)

**Категория:** `MomentumIndicators` • **Тип:** Компонент системы направленного движения Уайлдера

---

## Краткое описание
💡 **+DM (Positive Directional Movement)** — компонент системы направленного движения Уэллса Уайлдера, измеряющий силу восходящего движения цены за период. Рассчитывается на основе разницы максимумов соседних баров с учётом «выхода» текущего диапазона за пределы предыдущего. В отличие от простых осцилляторов, +DM фокусируется именно на направленности движения, а не на скорости. Используется исключительно в связке с -DM для построения индикаторов +DI, -DI и ADX.

## Формула расчёта

```math
+DM1 = \begin{cases} 
  High_t - High_{t-1}, & \text{если } (High_t - High_{t-1}) > 0 \text{ и } (High_t - High_{t-1}) > (Low_{t-1} - Low_t) \\
  0, & \text{иначе}
\end{cases}
```


Для периода `n > 1` применяется сглаживание по Уайлдеру:
```math
+DM_n = \frac{+DM_{n-1} \times (n-1) + +DM1_{текущий}}{n}
```

**Пояснение переменных:**
- `High_t`, `Low_t` — максимум и минимум текущего бара
- `+DM1` — однопериодное значение направленного движения
- `n` — период сглаживания (`optInTimePeriod`)

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inHigh` | `ReadOnlySpan<T>` | — | Массив цен максимумов (High) баров |
| `inLow` | `ReadOnlySpan<T>` | — | Массив цен минимумов (Low) баров |
| `inRange` | `Range` | весь массив | Диапазон индексов для расчёта (опционально) |
| `optInTimePeriod` | `int` | `14` | Период сглаживания: чем больше — сильнее сглаживание и запаздывание, но меньше шума |
| `outReal` | `Span<T>` | — | Выходной массив для значений +DM |
| `outRange` | `out Range` | — | Диапазон валидных индексов в `outReal` |

## Результат расчёта
- **`outReal`** содержит значения +DM: положительные числа отражают силу восходящего движения, нули — его отсутствие.
- **Период запаздывания (lookback):** `optInTimePeriod + unstablePeriod - 1` (для периода > 1). Первое валидное значение появляется только после накопления данных за указанный период.
- **Интерпретация:** Высокие значения +DM указывают на устойчивый восходящий тренд; нулевые значения — на боковое движение или отсутствие направленности.

## Практическое применение
✅ **Для чего использовать**
• Оценка силы восходящего импульса в рамках системы Уайлдера
• Формирование входа при доминировании +DM над -DM (через +DI > -DI)
• Фильтрация ложных пробоев: рост +DM подтверждает пробой сопротивления
• Расчёт композитных индикаторов (+DI, ADX) для оценки трендовости рынка

💡 **Идеи для стратегий**
1. **Пересечение DI:** Покупка при +DI > -DI и росте ADX выше 25.
2. **Дивергенция +DM:** Рост цены без роста +DM — сигнал ослабления тренда.
3. **Фильтр волатильности:** Торговля только при +DM > порогового значения (например, 0.5×среднее значение за 20 периодов).
4. **Комбинация с ATR:** Усиление сигнала при одновременном росте +DM и ATR.

⚠️ **Ограничения**
• Не работает на флэтах: генерирует много нулевых значений.
• Запаздывает на разворотах из-за сглаживания.
• Требует обязательного использования с -DM для интерпретации.
• Чувствителен к шуму на малых периодах (`optInTimePeriod < 7`).

## Сравнение с аналогами
| Индикатор | Скорость реакции | Запаздывание | Шумоустойчивость | Назначение |
|-----------|------------------|--------------|------------------|------------|
| **+DM** | Средняя | Среднее | Высокая (при периоде ≥14) | Измерение силы восходящего движения |
| **ROC** | Высокая | Низкое | Низкая | Измерение скорости изменения цены |
| **Momentum** | Высокая | Низкое | Средняя | Оценка ускорения тренда |
| **+DI** | Низкая | Высокое | Очень высокая | Нормализованный +DM относительно истинного диапазона |

## Теги
#индикатор/импульс #направленноедвижение #Уайлдер #тренд #TALib #PlusDM