# Tanh — Гиперболический тангенс (Tanh)

**Категория:** `MathTransform` • **Тип:** Нелинейное математическое преобразование

---

## Краткое описание
💡 Применяет гиперболическую функцию тангенса к каждому значению входного временного ряда. Результат всегда лежит в строгом диапазоне **[-1, 1]**, что делает его удобным инструментом для нормализации данных. В отличие от линейных преобразований (например, Min-Max), Tanh обеспечивает нелинейное «сжатие» экстремальных значений к границам диапазона. Не используется в классическом техническом анализе, но востребован в алгоритмической торговле для подготовки данных перед подачей в модели.

## Формула расчёта
```math
y = tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}
```
- **x** — исходное значение (цена, объём, значение другого индикатора)  
- **y** — нормализованное значение в диапазоне [-1, 1]  
- При |x| > 3 значение стремится к ±1, что подавляет влияние выбросов

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд (цены, объёмы или значения другого индикатора) |
| `inRange` | `Range` | весь массив | Диапазон индексов для обработки. Если не указан — обрабатывается весь массив |
| `outReal` | `Span<T>` | — | Выходной массив с результатами преобразования (длина = количеству обработанных точек) |
| `outRange` | `out Range` | — | Диапазон индексов исходных данных, для которых рассчитаны валидные значения |

## Результат расчёта
- **Выходные данные:** `outReal` содержит значения `tanh(inReal[i])` для каждого элемента входного диапазона
- **Lookback:** `0` — расчёт не требует исторических данных, первое значение рассчитывается мгновенно
- **Интерпретация:** значения близкие к +1/-1 указывают на сильные отклонения исходных данных от нуля; значения около 0 — на нейтральные/средние состояния

## Практическое применение
✅ **Для чего использовать**
- Нормализация входных данных для нейросетей и ML-моделей
- Подавление влияния экстремальных значений (выбросов) в исследовательских моделях
- Создание нелинейных фильтров для сглаживания шума
- Подготовка признаков (фич) для алгоритмических стратегий

💡 **Идеи для стратегий**
1. Нормализация индикатора (например, RSI) через `Tanh(RSI - 50)` для центрирования вокруг нуля и усиления экстремумов
2. Комбинация с линейными преобразованиями: `Tanh(EMA(Price) - Price)` для выявления сильных отклонений от тренда
3. Фильтрация сигналов: разрешать сделки только при `|Tanh(value)| > 0.7` для отбора «сильных» движений
4. Подготовка входных данных для рекуррентных нейросетей (LSTM) в системах прогнозирования

⚠️ **Ограничения**
- Не генерирует торговые сигналы напрямую — требует комбинации с другими инструментами
- Чувствителен к масштабу входных данных (рекомендуется предварительное центрирование)
- В условиях бокового рынка может создавать ложные экстремумы из-за накопления шума

## Сравнение с аналогами
| Функция | Диапазон | Нелинейность | Подавление выбросов | Типичное применение |
|---------|----------|--------------|---------------------|---------------------|
| **Tanh** | [-1, 1] | Высокая | Сильное | Нормализация для нейросетей |
| **Sigmoid** | [0, 1] | Высокая | Сильное | Бинарная классификация |
| **Softsign** | [-1, 1] | Умеренная | Умеренное | Альтернатива Tanh с более плавным градиентом |
| **Z-Score** | (-∞, +∞) | Линейная | Нет | Статистическая нормализация |

## Теги
#индикатор/математика #нормализация #машинноеобучение #TALib #Tanh