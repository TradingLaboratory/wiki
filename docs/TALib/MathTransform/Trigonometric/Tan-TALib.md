# Tan — Тригонометрический тангенс (TAN)

**Категория:** `MathTransform` • **Тип:** Математическое преобразование временного ряда

---

## Краткое описание
💡 Применяет тригонометрическую функцию тангенса к каждому элементу входного временного ряда. Не является классическим торговым индикатором — используется исключительно в экспериментальных или кастомных моделях анализа. Требует осторожности: тангенс не определён для углов ±90° + 180°·k, что может приводить к бесконечным значениям при определённых входных данных.

## Формула расчёта
```
outReal[i] = tan(inReal[i])
```
Где:
- `inReal[i]` — входное значение (в радианах)
- `outReal[i]` — результат применения тангенса

> ⚠️ Входные данные должны интерпретироваться как углы в радианах. Для ценовых данных требуется предварительное нормирование или преобразование.

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд (цены, объёмы или другие нормированные значения) |
| `inRange` | `Range` | весь массив | Диапазон индексов во входных данных для обработки |
| `outReal` | `Span<T>` | — | Выходной массив с результатами расчёта тангенса |
| `outRange` | `out Range` | — | Диапазон валидных индексов в выходных данных (обычно совпадает с `inRange`) |

## Результат расчёта
- **Выходные данные:** `outReal` содержит значения тангенса для каждого элемента входного диапазона
- **Lookback-период:** `0` — расчёт не требует исторических данных, применяется поэлементно
- **Интерпретация:** Каждый элемент `outReal[i]` соответствует `tan(inReal[inRange.Start + i])`. При некорректных входных данных (углы ±90° + 180°·k) возможны бесконечные значения (`±∞`).

## Практическое применение
✅ **Для чего использовать**
• Нормализация данных в гибридных моделях машинного обучения
• Создание нелинейных преобразований для экспериментальных осцилляторов
• Генерация синусоидальных компонент в кастомных циклических индикаторах

💡 **Идеи для стратегий**
1. Комбинирование с нормированным RSI: `tan((RSI - 50) / 50 * π/4)` для усиления экстремумов
2. Создание «перегретости» рынка: резкие скачки тангенса при приближении нормированного значения к ±1.0
3. Фильтрация шума: применение только к сглаженным данным (например, после SMA) для избежания разрывов

⚠️ **Ограничения**
• Не применим напрямую к ценам без предварительного нормирования в диапазон [-π/2; π/2]
• Чувствителен к выбросам: небольшие изменения входа у полюсов вызывают резкие скачки выхода
• Не рекомендуется для самостоятельного использования в торговых системах

## Сравнение с аналогами
| Функция | Нелинейность | Риск разрывов | Типичное применение |
|---------|--------------|---------------|---------------------|
| `Tan`   | Высокая      | ⚠️ Высокий    | Экспериментальные модели |
| `Sin`/`Cos` | Средняя   | ✅ Низкий     | Циклический анализ |
| `Log`   | Умеренная    | ✅ Низкий     | Нормализация волатильности |

## Теги
#индикатор/математика #преобразование #экспериментальный #TALib #Tan