# Obv — Балансовый объём (OBV)

**Категория:** `VolumeIndicators` • **Тип:** Кумулятивный индикатор потока объёма

---

## Краткое описание

💡 Индикатор отслеживает накопленный поток объёма, добавляя объём при росте цены и вычитая при падении. Показывает, «втекает» или «вытекает» деньги из актива, опережая ценовые движения. Используется для подтверждения трендов и выявления скрытых фаз накопления/распределения. Не генерирует прямые торговые сигналы — требует комбинации с ценовыми паттернами или другими индикаторами.

## Формула расчёта
```
OBV₀ = Volume₀
OBVᵢ = OBVᵢ₋₁ + Volumeᵢ, если Closeᵢ > Closeᵢ₋₁
OBVᵢ = OBVᵢ₋₁ - Volumeᵢ, если Closeᵢ < Closeᵢ₋₁
OBVᵢ = OBVᵢ₋₁, если Closeᵢ = Closeᵢ₋₁
```
**Пояснение:**  
- `OBVᵢ` — накопленное значение на периоде *i*  
- `Volumeᵢ` — объём торгов на периоде *i*  
- `Closeᵢ` — цена закрытия текущего периода  

*Пример:* При ценах [100, 102, 101] и объёмах [1000, 1500, 800]:  
OBV₀ = 1000 → OBV₁ = 1000 + 1500 = 2500 → OBV₂ = 2500 - 800 = 1700

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Цены закрытия (`Close`) для сравнения динамики |
| `inVolume` | `ReadOnlySpan<T>` | — | Объёмы торгов (`Volume`) за соответствующие периоды |
| `inRange` | `Range` | весь массив | Диапазон обработки данных (начальный/конечный индексы) |
| `outReal` | `Span<T>` | — | Выходной массив с кумулятивными значениями OBV |
| `outRange` | `out Range` | — | Диапазон индексов с валидными значениями в `outReal` |

## Результат расчёта
- **Возврат:** `RetCode.Success` при успешном расчёте  
- **Lookback:** `0` — первое значение рассчитывается на первом баре без задержки  
- **Интерпретация:**  
  • Рост OBV = положительный поток объёма (накопление)  
  • Падение OBV = отрицательный поток (распределение)  
  • Плоский участок = отсутствие значимого движения объёма  

## Практическое применение
✅ **Для чего использовать**  
• Подтверждение силы тренда: рост цены + рост OBV = устойчивый тренд вверх  
• Выявление дивергенций: цена обновляет максимум, но OBV — нет → слабость тренда  
• Определение фаз накопления перед прорывом (боковое движение цены + рост OBV)  
• Фильтрация ложных пробоев: пробой уровня без роста OBV часто ложный  

💡 **Идеи для стратегий**  
1. **Дивергенция + пробой:** При бычьей дивергенции (цена ↓, OBV ↑) ждём пробоя сопротивления цены с одновременным ростом OBV — сигнал на покупку.  
2. **Подтверждение тренда:** Открывать позицию только при совпадении направления цены и наклонного тренда OBV (линейная регрессия на OBV).  

⚠️ **Ограничения**  
• Резкие всплески объёма (например, новости) искажают тренд индикатора  
• На боковике даёт много ложных сигналов из-за «пилообразного» движения  
• Не учитывает ценовую волатильность — одинаково трактует изменение на 0.1% и 5%  

## Сравнение с аналогами
| Индикатор | Скорость реакции | Шумоустойчивость | Особенность |
|-----------|------------------|------------------|-------------|
| **OBV** | Высокая (без задержки) | Низкая | Чувствителен к единичным всплескам объёма |
| **Accumulation/Distribution** | Средняя | Выше | Учитывает позицию цены в диапазоне бара |
| **Volume Weighted MA** | Низкая (сглаживание) | Высокая | Фильтрует шум, но запаздывает |

## Теги
#индикатор/объём #поток_денег #накопление #дивергенция #TALib #Obv