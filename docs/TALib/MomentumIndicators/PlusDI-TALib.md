# PlusDI — Индикатор положительного направленного движения (+DI)

**Категория:** `MomentumIndicators` • **Тип:** Компонент системы направленного движения

---

## Краткое описание
💡 **+DI (Plus Directional Indicator)** — часть системы направленного движения Уэллса Уайлдера, измеряющая силу восходящего импульса цены. Рассчитывается на основе разницы между текущими и предыдущими максимумами с учётом истинного диапазона волатильности. В отличие от простых осцилляторов, +DI учитывает ценовые гэпы и фильтрует шум через сглаживание Уайлдера. Используется совместно с -DI и ADX для комплексной оценки направления и силы тренда.

## Формула расчёта
```text
+DM1 = max(High[t] - High[t-1], 0)  если (High[t] - High[t-1]) > |Low[t] - Low[t-1]|
TR1  = max(High[t] - Low[t], |High[t] - Close[t-1]|, |Low[t] - Close[t-1]|)

+DM(n) = Previous +DM(n) - (Previous +DM(n) / TimePeriod) + Today's +DM1
TR(n)  = Previous TR(n)  - (Previous TR(n)  / TimePeriod) + Today's TR1

+DI = (+DM(n) / TR(n)) * 100
```
**Пояснение переменных:**
- `+DM1` — однопериодное положительное направленное движение
- `TR1` — истинный диапазон (учитывает гэпы через цену закрытия предыдущего бара)
- `+DM(n)`, `TR(n)` — сглаженные значения за период `n` по методу Уайлдера
- Результат выражается в процентах (0–100%)

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inHigh` | `ReadOnlySpan<T>` | — | Максимальные цены (High) каждого периода |
| `inLow` | `ReadOnlySpan<T>` | — | Минимальные цены (Low) каждого периода |
| `inClose` | `ReadOnlySpan<T>` | — | Цены закрытия (Close) для расчёта истинного диапазона |
| `inRange` | `Range` | весь массив | Диапазон индексов входных данных для обработки |
| `optInTimePeriod` | `int` | `14` | Период сглаживания: чем больше — сильнее сглаживание и запаздывание, но меньше шума |
| `outReal` | `Span<T>` | — | Выходной массив со значениями +DI (только валидные данные) |
| `outRange` | `out Range` | — | Диапазон индексов входных данных, для которых рассчитаны значения |

## Результат расчёта
- **Возвращаемое значение:** `outReal` содержит процентные значения +DI (0–100%), где рост указывает на усиление восходящего импульса.
- **Lookback период:** `optInTimePeriod + UnstablePeriod` (обычно ≈ `optInTimePeriod`). Первое валидное значение появляется через `optInTimePeriod` баров от начала данных.
- **Интерпретация:** Значения `outReal[i]` соответствуют бару `inHigh[outRange.Start + i]`. При недостатке данных (`длина < lookback`) возвращается пустой диапазон `[0, -1]`.

## Практическое применение
✅ **Для чего использовать**
- Определение доминирования восходящего движения при сравнении с -DI (+DI > -DI → восходящий тренд)
- Фильтрация ложных пробоев: рост +DI подтверждает силу бычьего движения
- Комбинация с ADX: высокий ADX + рост +DI = сильный восходящий тренд

💡 **Идеи для стратегий**
1. **Пересечение DI:** Покупка при пересечении +DI сверху вниз через -DI при ADX > 25
2. **Дивергенция:** Рост цены при падении +DI — сигнал ослабления импульса (разворот вниз)
3. **Зона поддержки:** Удержание +DI выше 20 на коррекциях в восходящем тренде — сигнал продолжения

⚠️ **Ограничения**
- Слабо работает на флэтах: +DI и -DI переплетаются без чёткого доминирования
- Запаздывание из-за сглаживания: сигналы появляются после формирования движения
- Требует минимум `optInTimePeriod` баров данных для расчёта первого значения

## Сравнение с аналогами
| Индикатор | Скорость реакции | Запаздывание | Учёт гэпов | Основное применение |
|-----------|------------------|--------------|------------|---------------------|
| **+DI** | Средняя | Среднее | ✅ Да (через TR) | Направление + сила тренда |
| **+DM** | Высокая | Низкое | ✅ Да | Чистое направленное движение (без нормализации) |
| **MACD** | Высокая | Низкое | ❌ Нет | Импульс и дивергенции |
| **Stochastic** | Очень высокая | Минимальное | ❌ Нет | Перекупленность/перепроданность |

## Теги
#индикатор/импульс #тренд #направленноедвижение #TALib #Wilder #PlusDI