# RocP — Процентное изменение (ROCP)

**Категория:** `MomentumIndicators` • **Тип:** Осциллятор импульса в дробном представлении

---

## Краткое описание

💡 Измеряет относительное изменение цены за заданный период как долю от предыдущего значения: `(Текущая цена − Цена N периодов назад) / Цена N периодов назад`. В отличие от классического ROC, не масштабируется на 100 — возвращает дробные значения (0.05 вместо 5%). Предназначен для оценки силы и скорости ценового движения без искажения масштаба, что упрощает сравнение активов с разной волатильностью.

## Формула расчёта
```
ROCP = (Close[t] - Close[t - n]) / Close[t - n]
```
- `Close[t]` — цена закрытия текущего бара  
- `Close[t - n]` — цена закрытия `n` периодов назад (`n = optInTimePeriod`)  
- Результат: дробное число (например, 0.03 = рост на 3%)

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд: цены закрытия (Close), объёмы или значения другого индикатора |
| `inRange` | `Range` | — | Диапазон индексов для обработки. Если не указан — обрабатывается весь массив |
| `optInTimePeriod` | `int` | `10` | Период сравнения: чем меньше значение — выше чувствительность к краткосрочным движениям, но больше шума |
| `outReal` | `Span<T>` | — | Выходной массив только с валидными значениями индикатора |
| `outRange` | `out Range` | — | Диапазон индексов исходных данных, для которых рассчитаны значения (первый валидный индекс = `optInTimePeriod`) |

## Результат расчёта

- **Выходные данные**: `outReal` содержит дробные значения изменения цены (положительные = рост, отрицательные = падение).  
- **Период запаздывания (lookback)**: `optInTimePeriod` баров. Первое валидное значение появляется на баре с индексом `n` (при периоде 10 — на 10-м баре).  
- **Интерпретация**: Значение `0.02` означает рост на 2% относительно цены 10 периодов назад; `-0.015` — падение на 1.5%.

## Практическое применение

✅ **Для чего использовать**
• Сравнение силы трендов между активами с разной ценой (дробное представление устраняет масштабный дисбаланс)
• Фильтрация ложных пробоев: сильный импульс подтверждает пробой уровня
• Оценка ускорения/замедления движения при комбинации с ценовыми уровнями
• Нормализация данных для машинного обучения (значения в диапазоне [-1; +1] при умеренной волатильности)

💡 **Идеи для стратегий**
1. **Дивергенция с ценой**: цена обновляет максимум, но ROCP формирует более низкий пик → сигнал разворота.
2. **Кросс через ноль**: покупка при переходе из отрицательной в положительную зону + подтверждение объёмом.

⚠️ **Ограничения**
• Высокая чувствительность к выбросам (резкие движения искажают расчёт из-за деления на малое `Close[t-n]`)
• Не учитывает волатильность — одинаковое значение 0.03 может быть сильным сигналом для акции и слабым для криптовалюты
• Запаздывает на `optInTimePeriod` баров — не подходит для скальпинга без дополнительной фильтрации

## Сравнение с аналогами
| Индикатор | Масштаб | Запаздывание | Шумоустойчивость | Особенность |
|-----------|---------|--------------|------------------|-------------|
| **ROCP** | Дробный (0.05) | Среднее | Средняя | Универсален для сравнения активов |
| **ROC** | Процентный (5.0) | Среднее | Средняя | Привычен для визуального анализа |
| **MOM** | Абсолютный | Среднее | Низкая | Зависит от цены актива |
| **RSI** | Нормализованный (0–100) | Высокое | Высокая | Учитывает внутреннюю структуру движения |

## Теги

#индикатор/импульс 
#осциллятор 
#сравнительныйанализ 
#TALib 
#RocP