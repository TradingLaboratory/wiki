# WillR — Индекс Уильямса %R (Williams %R)

**Категория:** `MomentumIndicators` • **Тип:** Осциллятор перекупленности/перепроданности

---

## Краткое описание
💡 Индекс Уильямса %R измеряет позицию цены закрытия относительно диапазона между максимальной и минимальной ценами за заданный период. В отличие от RSI, использует инвертированную шкалу (0 вверху, -100 внизу) и работает с тремя ценовыми уровнями (High/Low/Close). Предназначен для выявления экстремальных рыночных условий и потенциальных разворотов в боковых трендах.

## Формула расчёта
```math
%R = ((HH - Close) / (HH - LL)) × -100
```
где:
- `HH` — Highest High (максимальная цена High за период)
- `LL` — Lowest Low (минимальная цена Low за период)
- `Close` — цена закрытия текущего бара

Пример для периода 14: если за 14 баров максимум = 105, минимум = 95, а текущий Close = 104, то %R = ((105-104)/(105-95)) × -100 = -10 (зона перекупленности).

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inHigh` | `ReadOnlySpan<T>` | — | Массив максимальных цен (High) каждого бара |
| `inLow` | `ReadOnlySpan<T>` | — | Массив минимальных цен (Low) каждого бара |
| `inClose` | `ReadOnlySpan<T>` | — | Массив цен закрытия (Close) каждого бара |
| `inRange` | `Range` | весь массив | Диапазон входных данных для расчёта (опционально) |
| `optInTimePeriod` | `int` | `14` | Период поиска экстремумов: чем меньше — выше чувствительность, но больше ложных сигналов |
| `outReal` | `Span<T>` | — | Выходной массив со значениями индикатора (-100…0) |
| `outRange` | `out Range` | — | Диапазон индексов с валидными значениями в `outReal` |

## Результат расчёта
- Возвращает значения в диапазоне **-100…0** через `outReal`
- Период запаздывания: `optInTimePeriod - 1` (например, для периода 14 — первое значение появится на 13-м баре)
- Код возврата `RetCode.Success` при успешном расчёте
- Выходной диапазон `outRange` указывает, для каких индексов входных данных получены валидные значения

## Практическое применение
✅ **Для чего использовать**
• Выявление зон перекупленности (> -20) и перепроданности (< -80)
• Поиск дивергенций между ценой и индикатором для раннего обнаружения разворотов
• Фильтрация сигналов в боковых рынках (флэте)
• Подтверждение точек входа при комбинации с трендовыми индикаторами

💡 **Идеи для стратегий**
1. **Классический вход по зонам**: покупка при выходе из зоны перепроданности (< -80 → выше -80) + подтверждение ростом объёмов; продажа при выходе из перекупленности (> -20 → ниже -20).
2. **Дивергенция + уровень -50**: бычья дивергенция + пересечение %R уровня -50 снизу вверх → сигнал на покупку.

⚠️ **Ограничения**
• Даёт ложные сигналы в сильном тренде (цена может долго оставаться в зоне перекупленности/перепроданности)
• Требует подтверждения другими инструментами — сам по себе не определяет направление тренда
• Меньшие периоды (< 10) усиливают шум, большие (> 20) увеличивают запаздывание

## Сравнение с аналогами
| Индикатор | Шкала | Входные данные | Чувствительность к экстремумам |
|-----------|-------|----------------|-------------------------------|
| **Williams %R** | -100…0 (инверт.) | High/Low/Close | Высокая (реагирует на абсолютные экстремумы) |
| **RSI** | 0…100 | Close | Средняя (учитывает импульс роста/падения) |
| **Stochastic** | 0…100 | High/Low/Close | Средняя (сглаживает экстремумы через %K/%D) |

## Теги
#индикатор/осциллятор #импульс #перекупленность #флэт #TALib #WillR