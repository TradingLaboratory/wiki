# Roc — Скорость изменения (ROC)

**Категория:** `MomentumIndicators` • **Тип:** Осциллятор импульса

---

## Краткое описание
⚡ Индикатор измеряет процентное изменение цены за заданный период, показывая силу и направление импульса. В отличие от скользящих средних, ROC сравнивает текущее значение напрямую с ценой из прошлого, что делает его чувствительным к резким движениям. Применяется для выявления ускорения/замедления тренда, а также зон перекупленности и перепроданности при боковом движении.

## Формула расчёта
```math
ROC = ((P_t / P_{t-n}) - 1) * 100
```
- `P_t` — текущая цена (обычно Close)  
- `P_{t-n}` — цена `n` периодов назад (`n = optInTimePeriod`)  
- Результат выражается в процентах: положительные значения = рост, отрицательные = падение

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд (цены закрытия Close или другие данные) |
| `inRange` | `Range` | весь массив | Диапазон индексов входных данных для обработки |
| `outReal` | `Span<T>` | — | Выходной массив **только с валидными значениями** индикатора |
| `outRange` | `out Range` | — | Диапазон индексов исходных данных, для которых рассчитаны значения (`Start = optInTimePeriod`) |
| `optInTimePeriod` | `int` | `10` | Период сравнения: чем меньше — выше чувствительность, но больше шума; чем больше — сильнее сглаживание и запаздывание |

## Результат расчёта
- Возвращает `RetCode.Success` при успешном расчёте и заполняет `outReal` + `outRange`
- **Lookback период:** равен `optInTimePeriod` — первое валидное значение появляется на баре №`n`
- Выходные данные центрированы относительно нуля: положительные значения = восходящий импульс, отрицательные = нисходящий

## Практическое применение
✅ **Для чего использовать**
- Выявление замедления тренда (дивергенция: цена растёт, ROC падает)
- Фильтрация ложных пробоев уровней поддержки/сопротивления
- Определение экстремумов в диапазоне (перекупленность/перепроданность при возврате к нулю)
- Подтверждение силы тренда при совместном использовании с трендовыми индикаторами

💡 **Идеи для стратегий**
1. **Дивергенция + тренд:** покупка при бычьей дивергенции (цена ниже минимума, ROC выше минимума) + цена выше 200-периодной SMA
2. **Возврат к нулю:** вход в лонг при пересечении ROC нулевой линии снизу вверх после периода отрицательных значений

⚠️ **Ограничения**
- Генерирует ложные сигналы в боковике без чётких границ
- Чувствителен к всплескам волатильности («шум») при малых периодах
- Не учитывает абсолютную величину цены — одинаковое % изменение на $10 и $100 выглядит идентично

## Сравнение с аналогами
| Индикатор | Скорость реакции | Запаздывание | Особенность |
|-----------|------------------|--------------|-------------|
| **ROC** | Высокая | Низкое (равно периоду) | Прямое сравнение цен, % изменение |
| **Momentum** | Высокая | Низкое | Абсолютная разница цен (не %) |
| **PPO** | Средняя | Выше | Сравнивает скользящие средние, а не цены |

## Теги
#индикатор/осциллятор #импульс #скоростьизменения #TALib #Roc