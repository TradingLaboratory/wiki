# MinusDM — Отрицательное направленное движение (-DM)

**Категория:** `MomentumIndicators` • **Тип:** Компонент системы направленного движения Уайлдера

---

## Краткое описание
💡 Отрицательное направленное движение (-DM) измеряет силу нисходящего импульса цены в системе Уайлдера. Индикатор фиксирует, насколько текущий минимум ниже предыдущего при одновременном отсутствии сильного восходящего движения. Используется исключительно как компонент для расчёта -DI и ADX — сам по себе не применяется в торговле. Ключевое отличие от простых осцилляторов: учитывает не абсолютное падение цены, а сравнение «внешних» частей ценовых диапазонов соседних баров.

## Формула расчёта
```text
-DM1 = Previous Low - Current Low, если:
       (Previous Low > Current Low) И 
       (Previous Low - Current Low) > (Current High - Previous High)
Иначе: -DM1 = 0

Для периода N > 1 (сглаживание Уайлдера):
-DM(N) = Previous -DM(N) - (Previous -DM(N) / N) + Current -DM1
```
**Пояснение:**
- `Previous Low/High` — минимум/максимум предыдущего бара
- `Current Low/High` — минимум/максимум текущего бара
- `N` — период сглаживания (`optInTimePeriod`)

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inHigh` | `ReadOnlySpan<T>` | — | Массив максимумов (High) за каждый период |
| `inLow` | `ReadOnlySpan<T>` | — | Массив минимумов (Low) за каждый период |
| `inRange` | `Range` | весь массив | Диапазон обработки входных данных (начальный/конечный индексы) |
| `optInTimePeriod` | `int` | `14` | Период сглаживания: чем больше — сильнее сглаживание и запаздывание, но меньше шума |
| `outReal` | `Span<T>` | — | Выходной массив со значениями -DM (только валидные данные) |
| `outRange` | `out Range` | — | Диапазон индексов входных данных, для которых рассчитаны значения |

## Результат расчёта
- Возвращает массив значений `-DM` в `outReal` и диапазон их соответствия входным данным в `outRange`
- **Lookback-период:** `optInTimePeriod + unstablePeriod - 1` (для периода > 1). Для периода = 1: 1 бар
- Первые `lookback` баров не имеют валидных значений — индикатор требует истории для инициализации
- Значения всегда ≥ 0: нулевое значение означает отсутствие нисходящего направленного движения

## Практическое применение
✅ **Для чего использовать**
• Расчёт индикатора направленности `-DI` совместно с `+DM`
• Построение системы направленного движения (включая ADX) для оценки силы тренда
• Фильтрация ложных пробоев: высокий `-DM` подтверждает силу нисходящего движения
• Анализ дивергенций между `-DM` и ценой для выявления ослабления нисходящего импульса

💡 **Идеи для стратегий**
1. **Пересечение +DI/-DI:** покупка при `+DI > -DI` и продажа при `-DI > +DI` (требует подтверждения через ADX > 25)
2. **Дивергенция:** рост цены при снижении `-DM` — сигнал ослабления медвежьего давления перед разворотом вверх

⚠️ **Ограничения**
• Не работает на флэте: генерирует череду нулевых значений при отсутствии направленного движения
• Запаздывает при резких разворотах из-за сглаживания
• Требует комбинации с `+DM` и `TR` — сам по себе не даёт торговых сигналов

## Сравнение с аналогами
| Индикатор | Скорость реакции | Запаздывание | Шумоустойчивость | Назначение |
|-----------|------------------|--------------|------------------|------------|
| **-DM** | Средняя | Среднее (зависит от периода) | Высокая (сглаживание Уайлдера) | Измерение силы нисходящего движения |
| **-DI** | Низкая | Высокое | Очень высокая | Нормализованный показатель силы тренда |
| **ROC** | Высокая | Низкое | Низкая | Измерение скорости изменения цены |

## Теги
#индикатор/импульс #направленноедвижение #тренд #TALib #Wilder #MinusDM