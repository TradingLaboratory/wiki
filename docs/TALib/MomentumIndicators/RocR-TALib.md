# RocR — Коэффициент скорости изменения (ROCR)

**Категория:** `MomentumIndicators` • **Тип:** Осциллятор импульса на основе отношения цен

---

## Краткое описание

💡 Измеряет пропорциональное изменение цены как отношение текущего значения к значению за `N` периодов назад. В отличие от классического ROC, результат всегда положителен и центрирован относительно единицы (1.0), что упрощает интерпретацию пропорциональных изменений. Предназначен для оценки относительной силы тренда и выявления ускорения/замедления движения без искажений от знака изменения.

## Формула расчёта
```math
ROCR_t = \frac{Price_t}{Price_{t-n}}
```
Где:
- `Price_t` — текущая цена (или значение временного ряда)
- `Price_{t-n}` — цена `n` периодов назад (`n = optInTimePeriod`)
- Результат > 1.0 = рост, < 1.0 = падение, = 1.0 = стагнация

Пример для периода 10: если цена сегодня 110, а 10 дней назад — 100, то `ROCR = 110 / 100 = 1.10`.

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд: цены закрытия (Close), High/Low или значения другого индикатора |
| `inRange` | `Range` | весь массив | Диапазон индексов входных данных для расчёта. Если не указан — обрабатывается весь массив |
| `optInTimePeriod` | `int` | `10` | Период сравнения в барах. Увеличение снижает чувствительность и шум, но повышает запаздывание |
| `outReal` | `Span<T>` | — | Выходной массив: содержит только валидные значения индикатора (длина = `outRange.End - outRange.Start + 1`) |
| `outRange` | `out Range` | — | Диапазон индексов входных данных, для которых рассчитаны валидные значения (`Start` = первый валидный индекс) |

## Результат расчёта
- **`outReal`** — массив отношений `текущая цена / цена n периодов назад`. Значения интерпретируются относительно 1.0.
- **Период запаздывания:** `lookback = optInTimePeriod`. Первое валидное значение появляется на баре с индексом `optInTimePeriod`.
- **Защита от деления на ноль:** при нулевом значении в знаменателе возвращается 0.

## Практическое применение
✅ **Для чего использовать**
• Оценка относительной силы тренда без искажений от абсолютных значений цены
• Сравнение динамики разных активов (нормализованный показатель)
• Фильтрация ложных пробоев: рост цены без роста ROCR указывает на слабый импульс
• Построение осцилляторов второго порядка (например, скользящая средняя от ROCR)

💡 **Идеи для стратегий**
1. **Подтверждение тренда:** вход в лонг при `ROCR(10) > 1.02` и росте объёмов; выход при возврате к 1.0.
2. **Дивергенция:** цена обновляет максимум, но `ROCR(20)` формирует более низкий пик → сигнал разворота.

⚠️ **Ограничения**
• Не работает на флетовых участках (значения колеблются около 1.0 без чётких сигналов)
• Чувствителен к всплескам волатильности при малых периодах (`optInTimePeriod < 5`)
• Требует дополнительной фильтрации для использования в скальпинге

## Сравнение с аналогами
| Индикатор | Центрирование | Знак | Формула | Лучшее применение |
|-----------|---------------|------|---------|-------------------|
| **ROCR** | 1.0 | Всегда + | `Price_t / Price_{t-n}` | Сравнение пропорциональных изменений |
| **ROC** | 0 | ± | `(Price_t - Price_{t-n}) / Price_{t-n}` | Классический анализ импульса |
| **ROCP** | 0 | ± | `ROC × 100` | Процентное изменение в пунктах |

## Теги
#индикатор/импульс #осциллятор #относительная_сила #TALib #RocR