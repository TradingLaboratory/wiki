# SarExt — Расширенный параболический SAR (Parabolic SAR Extended)

**Категория:** `OverlapStudies` • **Тип:** Трендовый индикатор с динамическим стоп-лоссом

---

## Краткое описание
⚡ Расширенная версия классического параболического индикатора Stop and Reverse с независимой настройкой параметров для длинных и коротких позиций. Точки индикатора формируют динамические уровни стоп-лосса, следуя за трендом с ускорением при новых экстремумах. Идеален для трендовых рынков и систем с трейлинг-стопом.

## Формула расчёта
```text
SARₜ = SARₜ₋₁ + AF × (EP - SARₜ₋₁)
```
Где:
- `SARₜ` — текущее значение индикатора
- `AF` — фактор ускорения (растёт при каждом новом экстремуме до максимума)
- `EP` — экстремальная точка (максимум цены для Long, минимум для Short)

При развороте: `SAR` сбрасывается к значению `EP` с применением смещения `optInOffsetOnReverse`.

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inHigh` | `ReadOnlySpan<T>` | — | Массив максимальных цен (High) |
| `inLow` | `ReadOnlySpan<T>` | — | Массив минимальных цен (Low) |
| `optInStartValue` | `double` | `0.0` | Начальное значение SAR: >0 — Long, <0 — Short, 0 — автоопределение по первым двум барам |
| `optInOffsetOnReverse` | `double` | `0.0` | Смещение при развороте: увеличивает (+) или уменьшает (-) расстояние между SAR и ценой после смены тренда |
| `optInAccelerationInitLong` | `double` | `0.02` | Начальный фактор ускорения для длинных позиций (чувствительность в начале тренда) |
| `optInAccelerationLong` | `double` | `0.02` | Инкремент ускорения для длинных позиций (шаг роста AF при новых максимумах) |
| `optInAccelerationMaxLong` | `double` | `0.2` | Максимальный фактор ускорения для длинных позиций |
| `optInAccelerationInitShort` | `double` | `0.02` | Начальный фактор ускорения для коротких позиций |
| `optInAccelerationShort` | `double` | `0.02` | Инкремент ускорения для коротких позиций |
| `optInAccelerationMaxShort` | `double` | `0.2` | Максимальный фактор ускорения для коротких позиций |

## Результат расчёта
- **Выходные значения** (`outReal`): положительные числа = позиция Long (SAR ниже цены), отрицательные = позиция Short (SAR выше цены)
- **Lookback период**: 1 бар (минимальное количество данных для первого расчёта)
- **Валидный диапазон** (`outRange`): индексы входных баров, для которых рассчитаны корректные значения

## Практическое применение
✅ **Для чего использовать**
• Динамический трейлинг-стоп для фиксации прибыли в тренде
• Визуализация направления тренда (точки ниже цены = восходящий, выше = нисходящий)
• Фильтрация ложных пробоев в волатильных рынках за счёт настройки ускорения
• Генерация сигналов разворота при пересечении цены и SAR

💡 **Идеи для стратегий**
1. **Трендовая система**: вход в Long при формировании точки SAR ниже цены после разворота, выход при пересечении цены и SAR сверху вниз.
2. **Адаптивный стоп-лосс**: использовать значение SAR как динамический стоп для открытых позиций с настройкой ускорения под волатильность актива.

⚠️ **Ограничения**
• Неэффективен во флэте — генерирует частые ложные развороты
• Запаздывает на сильных импульсах из-за инерции фактора ускорения
• Требует тонкой настройки параметров под конкретный актив и таймфрейм

## Сравнение с аналогами
| Индикатор | Чувствительность | Запаздывание | Гибкость настройки |
|-----------|------------------|--------------|---------------------|
| **SarExt** | Высокая (настраивается) | Низкое | ⭐⭐⭐⭐⭐ (раздельные параметры Long/Short) |
| Классический SAR | Фиксированная | Среднее | ⭐⭐ (единый набор параметров) |
| ATR Trailing Stop | Средняя | Низкое | ⭐⭐⭐ (зависит от волатильности) |

## Теги
#индикатор/наложение #тренд #стоп-реверс #трейлинг #TALib #SarExt