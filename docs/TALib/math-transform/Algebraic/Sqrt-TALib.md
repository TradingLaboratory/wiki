# Sqrt — Квадратный корень (Math Transform)

**Категория:** `MathTransform` • **Тип:** Базовое математическое преобразование временного ряда

---

## Краткое описание
💡 Применяет операцию квадратного корня к каждому элементу входного временного ряда без задержки и сглаживания. Не является самостоятельным торговым индикатором, а служит строительным блоком для расчёта волатильности (стандартное отклонение), нормализации данных и статистических метрик риска. Преобразование уменьшает масштаб значений и частично нормализует распределение, улучшая интерпретируемость показателей.

## Формула расчёта
```
outReal[i] = √(inReal[i])
```
**Пояснение:**  
- `inReal[i]` — исходное значение на баре *i* (цена, объём, значение другого индикатора)  
- `outReal[i]` — результат применения квадратного корня к этому значению  
- Требование: `inReal[i] ≥ 0` (для отрицательных чисел результат не определён в рамках финансовых расчётов)

## Параметры метода
| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|------------------------|----------|
| `inReal` | `ReadOnlySpan<T>` | — | Входной временной ряд: цены закрытия, волатильность, значения других индикаторов (только неотрицательные) |
| `inRange` | `Range` | весь массив | Диапазон индексов входных данных для обработки. Если не указан — обрабатывается весь массив |
| `outReal` | `Span<T>` | — | Выходной массив с результатами √(inReal[i]) только для валидных элементов диапазона |
| `outRange` | `out Range` | — | Диапазон индексов в исходных данных, для которых получены валидные результаты |
| `T` | generic | `float` / `double` | Числовой тип с поддержкой IEEE 754 (обычно float или double) |

## Результат расчёта
- Возвращает массив значений квадратного корня для каждого элемента входного диапазона
- **Lookback = 0** — расчёт мгновенный, не требует исторических данных (`SqrtLookback() => 0`)
- Выходной диапазон `outRange` всегда начинается с `startIdx` входного диапазона и заканчивается последним обработанным элементом
- Код возврата: `Success` при валидных данных, `OutOfRangeParam` при некорректном диапазоне

## Практическое применение
✅ **Для чего использовать**
• Расчёт волатильности: квадратный корень применяется в формуле стандартного отклонения (σ = √(дисперсия))
• Нормализация распределения данных для уменьшения влияния экстремальных значений
• Предварительная обработка данных перед построением сложных индикаторов (например, ATR на основе √(среднего истинного диапазона))
• Масштабирование значений для совместимости с другими математическими преобразованиями

💡 **Идеи для стратегий**
1. **Волатильность на основе преобразования**: примените `Sqrt` к квадратам отклонений цен для расчёта стандартного отклонения вручную — полезно для кастомных фильтров волатильности.
2. **Нормализация объёмов**: преобразуйте объёмы через `Sqrt` перед использованием в индикаторах вроде OBV — снижает влияние всплесков объёмов на сигналы.

⚠️ **Ограничения**
• Не применим к отрицательным значениям (требует предварительной обработки данных)
• Не генерирует торговые сигналы напрямую — только вспомогательная операция
• Чувствителен к нулевым значениям (результат = 0), что может искажать интерпретацию при работе с нормализованными данными

## Сравнение с аналогами
| Преобразование | Скорость реакции | Запаздывание | Применение |
|----------------|------------------|--------------|------------|
| `Sqrt` | Мгновенная | 0 | Нормализация, расчёт волатильности |
| `Log` | Мгновенная | 0 | Сжатие динамического диапазона, анализ трендов |
| `SMA` | Зависит от периода | N-1 | Сглаживание, фильтрация шума |

## Теги
#индикатор/math-transform #математика #волатильность #нормализация #TALib #Sqrt